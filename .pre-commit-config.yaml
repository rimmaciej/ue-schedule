repos:
    - repo: local
      hooks:
          - id: flake8
            name: flake8
            entry: poetry run flake8
            types: [python]
            language: system
            stages: [push, commit]
          - id: mypy
            name: mypy
            entry: poetry run mypy
            types: [python]
            language: system
            stages: [push, commit]
          - id: pytest
            name: pytest
            entry: poetry run pytest
            types: [python]
            language: system
            pass_filenames: false
            always_run: true
            stages: [push, commit]
